import { CheckCircle2, Circle } from "lucide-react";
export function ValidationExperiment({ steps }: { steps?: string[] }) { const list=steps?.length?steps:["Publish a single-outcome landing page","Build a target prospect list","Send a 30-message outreach campaign","Run first interview questions","Ask for a paid preorder or pilot","Set success and failure criteria"];return <section className="validation-experiment"><div><p className="eyebrow">Validation experiment</p><h4>Earn the right to build.</h4><p>Success: qualified buyers commit time or money. Failure: the problem isn't painful enough to justify payment.</p></div><ol>{list.map((step,index)=><li key={step}><span>{index<2?<CheckCircle2 size={15}/>:<Circle size={15}/>}</span>{step}</li>)}</ol></section>}
