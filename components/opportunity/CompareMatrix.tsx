"use client";
import { useMemo, useState } from "react";
import { Check, Plus, X } from "lucide-react";
import { validationReports } from "@/lib/report-mocks";
export function CompareMatrix(){const [selected,setSelected]=useState(validationReports.slice(0,3).map(r=>r.opportunity.id));const reports=useMemo(()=>validationReports.filter(r=>selected.includes(r.opportunity.id)),[selected]);const toggle=(id:string)=>setSelected(s=>s.includes(id)?s.filter(x=>x!==id):s.length<4?[...s,id]:s);const metric=(key:"painSeverity"|"willingnessToPay"|"distributionClarity"|"retentionPotential"|"platformDependencyRisk"|"regulatoryRisk")=>(r:typeof reports[number])=>r.opportunity.scorecard.scores[key];return <section className="compare-engine"><div className="compare-library-note"><b>Sample report library</b><span>Illustrative opportunity data—use it to explore the decision framework, not as market proof.</span></div><div className="compare-picker">{validationReports.map(r=>{const active=selected.includes(r.opportunity.id);return <button key={r.id} className={active?"selected":""} onClick={()=>toggle(r.opportunity.id)}>{active?<Check size={14}/>:<Plus size={14}/>} {r.opportunity.name}</button>})}</div><div className="matrix-wrap"><table className="engine-matrix expanded-matrix"><thead><tr><th>Decision factor</th>{reports.map(r=><th key={r.id}><b>{r.opportunity.name}</b><small>{r.opportunity.targetCustomer}</small><button onClick={()=>toggle(r.opportunity.id)} aria-label={`Remove ${r.opportunity.name}`}><X size={13}/></button></th>)}</tr></thead><tbody><Row label="Final score" reports={reports} value={r=>`${r.opportunity.scorecard.total} / 100`}/><Row label="Verdict" reports={reports} value={r=>r.opportunity.scorecard.verdict}/><Row label="Pain severity" reports={reports} value={r=>metric("painSeverity")(r)}/><Row label="Willingness to pay" reports={reports} value={r=>metric("willingnessToPay")(r)}/><Row label="Build complexity" reports={reports} value={r=>r.opportunity.mvp.buildComplexity}/><Row label="Distribution ease" reports={reports} value={r=>metric("distributionClarity")(r)}/><Row label="Retention potential" reports={reports} value={r=>metric("retentionPotential")(r)}/><Row label="Platform risk" reports={reports} value={r=>`${metric("platformDependencyRisk")(r)} risk`}/><Row label="Regulatory risk" reports={reports} value={r=>`${metric("regulatoryRisk")(r)} risk`}/><Row label="Path to $500 MRR" reports={reports} value={r=>`${Math.ceil(500/79)} Starter customers`}/><Row label="Path to $3,000 MRR" reports={reports} value={r=>`${Math.ceil(3000/149)} Pro customers`}/><Row label="First validation experiment" reports={reports} value={r=>r.opportunity.launch.validationExperiment?.[0]??r.opportunity.launch.successMetric}/></tbody></table></div></section>}
function Row({label,reports,value}:{label:string;reports:typeof validationReports;value:(report:typeof validationReports[number])=>string|number}){return <tr><td>{label}</td>{reports.map(r=><td key={r.id}>{value(r)}</td>)}</tr>}
