"use client";
import { ArrowDown, ArrowUp, BadgeCheck, ShieldAlert } from "lucide-react";
import { scoringCriteria } from "@/lib/scoring";
import { OpportunityScorecard } from "@/lib/types";
export function ScoreBreakdown({ scorecard, previousScore }: { scorecard: OpportunityScorecard; previousScore?: number }) { const delta=previousScore===undefined?0:Math.round((scorecard.total-previousScore)*10)/10; return <section className="engine-card score-breakdown"><header><div><p className="eyebrow">Weighted decision</p><h3>Evidence-adjusted score</h3></div><div className="engine-score"><b>{scorecard.total}</b><span>/100</span></div></header><div className="score-summary"><span className={`engine-verdict ${scorecard.verdict.toLowerCase().replaceAll(" ","-")}`}>{scorecard.verdict}</span><span><BadgeCheck size={14}/>{scorecard.confidence}% evidence confidence</span>{delta!==0&&<span className={delta>0?"score-up":"score-down"}>{delta>0?<ArrowUp size={14}/>:<ArrowDown size={14}/>} {Math.abs(delta)} vs prior weighting</span>}</div><div className="criterion-bars">{scoringCriteria.map(c=>{const raw=scorecard.scores[c.key];const effective=c.risk?100-raw:raw;const evidence=scorecard.evidenceRefs[c.key]??[];return <article key={c.key}><div><b>{c.label}</b><span>{c.risk?`${raw} risk → ${effective} effective`:`${raw}/100`}</span></div><i><em style={{width:`${effective}%`}}/></i><p>{scorecard.notes[c.key]}</p>{evidence.length?<small>{evidence.length} linked evidence item{evidence.length===1?"":"s"}</small>:<small className="missing"><ShieldAlert size={12}/>Needs evidence</small>}</article>})}</div></section> }
