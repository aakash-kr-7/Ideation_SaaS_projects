# Loaded by the Supabase CLI for local Google OAuth.
# Keep the real values in the ignored .env file, never in source control.
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=your-google-oauth-web-client-id
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET=your-google-oauth-web-client-secret
