import Link from "next/link";
import { ArrowRight, LockKeyhole, Sparkles } from "lucide-react";
import { Brand } from "@/components/layout/brand";
export default function AuthPage(){return <main className="auth-page"><div className="auth-card"><Brand/><div className="auth-copy"><span className="auth-icon"><Sparkles size={20}/></span><p className="eyebrow">WELCOME TO BUILDSIGNAL</p><h1>Make your next idea earn its build time.</h1><p>Sign in is intentionally mocked for this foundation. Connect Supabase Auth here when you are ready.</p></div><button className="oauth">Continue with Google <ArrowRight size={16}/></button><button className="email">Continue with email</button><small><LockKeyhole size={13}/> No provider is connected in this demo.</small><Link href="/dashboard">Continue to demo workspace <ArrowRight size={14}/></Link></div></main>}
