import Link from "next/link";
import { Check, HelpCircle } from "lucide-react";
import { AppShell } from "@/components/layout/app-shell";
const plans=[{name:"Starter",price:"$0",intro:"For one idea worth validating.",items:["1 validation report / month","Fast Scan research mode","Core scoring and MVP plan","Evidence source panels"]},{name:"Builder",price:"$29",intro:"For founders running a serious loop.",items:["10 research runs / month","Deep Validation reports","Opportunity comparison","Exportable reports and plans"],featured:true},{name:"Studio",price:"$79",intro:"For partners and small product teams.",items:["Everything in Builder","Unlimited collaborators","Shared project workspace","Priority research queue"]}];
export default function PricingPage(){return <AppShell title="Pricing"><div className="page-content pricing-page"><div className="pricing-heading"><p className="eyebrow">Plans that match your pace</p><h2>Pay for clearer decisions, not inflated promises.</h2><p>Use BuildSignal to organize the work of validation. Evidence should inform your judgment—not replace it.</p></div><div className="plans">{plans.map(p=><article className={p.featured?"plan featured":"plan"} key={p.name}>{p.featured&&<span className="popular">MOST PRACTICAL</span>}<p>{p.name.toUpperCase()}</p><h3>{p.price}<small>/ month</small></h3><span>{p.intro}</span><Link href="/auth" className={p.featured?"button":"button ghost"}>{p.name==="Starter"?"Start free":"Choose Builder"}</Link><ul>{p.items.map(i=><li key={i}><Check size={15}/>{i}</li>)}</ul></article>)}</div><div className="billing-note"><HelpCircle size={18}/><div><b>Start with the work, not the plan.</b><p>Every plan is designed around a real validation workflow. Upgrade only when your research volume asks for it.</p></div></div></div></AppShell>}
