import Link from "next/link";
import { Check, CircleHelp } from "lucide-react";
import { AppShell } from "@/components/layout/app-shell";
import { productCopy } from "@/lib/copy";
const plans=[{name:"Free",price:"$0",intro:productCopy.pricing.Free,items:["1 fast scan / month","Limited evidence view","Explainable core score","No exports"]},{name:"Builder",price:"$19",intro:productCopy.pricing.Builder,items:["10 scans / month","3 deep reports / month","Markdown export","Evidence and scoring controls"],featured:true},{name:"Pro",price:"$49",intro:productCopy.pricing.Pro,items:["15 deep reports / month","Compare ideas","PDF export","Custom weights and GTM scripts"]},{name:"Studio",price:"$149",intro:productCopy.pricing.Studio,items:["50 reports / month","White-label exports","Client-ready reports","Team workspace"]}];
export default function PricingPage(){return <AppShell title="Pricing"><div className="page-content pricing-page"><div className="pricing-heading"><p className="eyebrow">Pricing that follows the work</p><h2>Pay for clearer product decisions, not louder promises.</h2><p>{productCopy.positioning}</p></div><div className="plans four-plans">{plans.map(plan=><article className={plan.featured?"plan featured":"plan"} key={plan.name}>{plan.featured&&<span className="popular">BEST FOR BUILDERS</span>}<p>{plan.name.toUpperCase()}</p><h3>{plan.price}<small>/ month</small></h3><span>{plan.intro}</span><Link href="/auth" className={plan.featured?"button":"button ghost"}>{plan.name==="Free"?"Get your first signal":`Choose ${plan.name}`}</Link><ul>{plan.items.map(item=><li key={item}><Check size={15}/>{item}</li>)}</ul></article>)}</div><div className="report-pass"><div><p className="eyebrow">One-time option</p><h3>Deep Report Pass <span>$29 / report</span></h3><p>{productCopy.pricing.Pass}</p></div><Link href="/research/new" className="button ghost">Run one deep report</Link></div><div className="billing-note"><CircleHelp size={18}/><div><b>Reports are decision support, not guarantees.</b><p>Inspect the sources, challenge the assumptions, and change the weights. A good report should make your next customer test clearer—not pretend to remove the risk.</p></div></div></div></AppShell>}
