import Link from "next/link";
import { Check, CircleHelp } from "lucide-react";
import { AppShell } from "@/components/layout/app-shell";
const plans=[{name:"Free",price:"$0",intro:"For a first decision.",items:["1 fast scan / month","Limited evidence view","Explainable core score","No exports"]},{name:"Builder",price:"$19",intro:"For independent builders validating deliberately.",items:["10 scans / month","3 deep reports / month","Markdown export","Evidence and scoring controls"],featured:true},{name:"Pro",price:"$49",intro:"For a repeatable research practice.",items:["15 deep reports / month","Compare ideas","PDF export","Custom weights and GTM scripts"]},{name:"Studio",price:"$149",intro:"For teams and client work.",items:["50 reports / month","White-label exports","Client-ready reports","Team workspace"]}];
export default function PricingPage(){return <AppShell title="Pricing"><div className="page-content pricing-page"><div className="pricing-heading"><p className="eyebrow">Pricing that follows the work</p><h2>Pay for clearer product decisions, not louder promises.</h2><p>Start with a focused scan. Move deeper when your research cadence, export needs, or client workflow asks for it.</p></div><div className="plans four-plans">{plans.map(plan=><article className={plan.featured?"plan featured":"plan"} key={plan.name}>{plan.featured&&<span className="popular">BEST FOR BUILDERS</span>}<p>{plan.name.toUpperCase()}</p><h3>{plan.price}<small>/ month</small></h3><span>{plan.intro}</span><Link href="/auth" className={plan.featured?"button":"button ghost"}>{plan.name==="Free"?"Start free":`Choose ${plan.name}`}</Link><ul>{plan.items.map(item=><li key={item}><Check size={15}/>{item}</li>)}</ul></article>)}</div><div className="report-pass"><div><p className="eyebrow">One-time option</p><h3>Deep Report Pass <span>$29 / report</span></h3><p>For one important decision without a recurring subscription.</p></div><Link href="/research/new" className="button ghost">Run a deep report</Link></div><div className="billing-note"><CircleHelp size={18}/><div><b>Reports are not guarantees.</b><p>Evidence informs a decision; it does not promise demand, revenue, or product-market fit. You can inspect sources and change the weights behind every score.</p></div></div></div></AppShell>}
