import { AppShell } from "@/components/layout/app-shell";
import { Bell, Database, KeyRound, ShieldCheck, UserRound } from "lucide-react";
const items=[{icon:UserRound,title:"Workspace profile",copy:"Builder workspace · Personal plan",action:"Edit profile"},{icon:Database,title:"Research data",copy:"Mock repository active. Connect Supabase to persist runs, evidence, and saved opportunities.",action:"Connect Supabase"},{icon:KeyRound,title:"API integration",copy:"A clean repository boundary is ready for your research workflow, source adapters, and report generation.",action:"View setup"},{icon:Bell,title:"Research notifications",copy:"Receive a summary when a long-running validation job is complete.",action:"Configure"}];
export default function SettingsPage(){return <AppShell title="Settings"><div className="page-content settings-page"><div className="page-lead"><p className="eyebrow">Workspace configuration</p><h2>Set up the system around your research practice.</h2><p>This foundation uses mock data and clean TypeScript contracts so each setting has a clear integration point.</p></div><div className="settings-list">{items.map(({icon:Icon,title,copy,action})=><article key={title}><span><Icon size={19}/></span><div><h3>{title}</h3><p>{copy}</p></div><button>{action}</button></article>)}</div><div className="security-note"><ShieldCheck size={19}/><div><b>Data handling principle</b><p>Keep source references with their evidence, make generated conclusions reviewable, and never represent a score as a revenue guarantee.</p></div></div></div></AppShell>}
