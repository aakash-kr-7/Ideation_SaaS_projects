import { AppShell } from "@/components/layout/app-shell";
import { Bell, Database, KeyRound, ShieldCheck, UserRound } from "lucide-react";
const items=[{icon:UserRound,title:"Workspace profile",copy:"Research workspace · Active environment. Configure your identity and team access.",action:"Edit profile"},{icon:Database,title:"Research data",copy:"Local repository active. Connect to a persistent data layer to retain research runs, evidence, and comparative analyses.",action:"Connect database"},{icon:KeyRound,title:"API integration",copy:"Clean integration boundaries are established for research workflows, source adapters, and report generation pipelines.",action:"View configuration"},{icon:Bell,title:"Research notifications",copy:"Receive a structured summary when a long-running analysis completes.",action:"Configure"}];
export default function SettingsPage(){return <AppShell title="Configuration"><div className="page-content settings-page"><div className="page-lead"><p className="eyebrow">Workspace configuration</p><h2>Configure your research environment.</h2><p>This workspace uses demonstration data and clean TypeScript contracts. Each setting has a defined integration point for production deployment.</p></div><div className="settings-list">{items.map(({icon:Icon,title,copy,action})=><article key={title}><span><Icon size={19}/></span><div><h3>{title}</h3><p>{copy}</p></div><button>{action}</button></article>)}</div><div className="security-note"><ShieldCheck size={19}/><div><b>Data handling principle</b><p>Source references remain attached to their evidence. Generated conclusions are inspectable. Scores are never represented as revenue guarantees.</p></div></div></div></AppShell>}
