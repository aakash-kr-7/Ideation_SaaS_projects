import { ValidationReport } from "./report-schema";
export function reportToMarkdown(report: ValidationReport) { const { opportunity:o }=report; const scoreRows=Object.entries(o.scorecard.scores).map(([key,value])=>`| ${key.replace(/([A-Z])/g," $1")} | ${value} | ${o.scorecard.notes[key as keyof typeof o.scorecard.notes]} |`).join("\n"); const evidenceRows=o.evidence.map(e=>`| ${e.source} | ${e.sourceType} | ${e.strength} | ${e.snippet.replaceAll("|","/")} |`).join("\n"); const competitorRows=o.competitors.map(c=>`| ${c.name} | ${c.target} | ${c.pricing} | ${c.strength} | ${c.gap} |`).join("\n"); return `# ${o.name}\n\n> SignalFit sample / validation report — decision support, not a revenue guarantee.\n\n**Report date:** ${report.generatedAt}  \n**Verdict:** ${o.scorecard.verdict}  \n**Weighted score:** ${o.scorecard.total}/100  \n**Evidence confidence:** ${o.scorecard.confidence}%\n\n---\n\n## Executive summary\n${report.executiveSummary}\n\n## Product thesis\n${o.oneLiner}\n\n## Target customer\n${o.targetCustomer}\n\n## Core pain\n${o.corePain}\n\n## Current workaround\n${o.currentWorkaround ?? "Not yet verified."}\n\n## Why users may pay\n${o.whyUsersPay ?? o.pricing.rationale}\n\n## Weighted score table\n| Criterion | Score | Reasoning |\n|---|---:|---|\n${scoreRows}\n\n## Evidence table\n| Source | Type | Quality | Extracted insight |\n|---|---|---|---|\n${evidenceRows}\n\n## Competitor teardown\n| Competitor | Target customer | Pricing | Strength | Gap to attack |\n|---|---|---|---|---|\n${competitorRows}\n\n## MVP roadmap\n### Version 0 — Validation\nLanding page, targeted prospect list, workflow interviews, and a paid pilot ask.\n\n### Version 1 — Core MVP\n${o.mvp.scope.map(item=>`- ${item}`).join("\n")}\n\n### Version 2 — Paid workflow\nAutomate only the steps proven through the paid concierge workflow.\n\n### Version 3 — Retention\nAdd retention features once the core outcome is used consistently.\n\n### Do not build first\n${(o.notToBuildFirst ?? o.mvp.exclusions).map(item=>`- ${item}`).join("\n")}\n\n## Pricing strategy\n| Offer | Price | Rationale |\n|---|---:|---|\n| First offer | ${o.pricing.firstOffer} | Prove paid demand early |\n| Core plan | ${o.pricing.pricePoint} | ${o.pricing.rationale} |\n\n## Risks\n${o.risks.map(r=>`- **${r.severity} ${r.category}:** ${r.description}\n  - Mitigation: ${r.mitigation}`).join("\n")}\n\n## Launch plan\n**First channel:** ${o.launch.firstCustomerChannel}\n\n### First 10 customers\n${o.launch.firstTenStrategy.map((s,i)=>`${i+1}. ${s}`).join("\n")}\n\n### First 100 users\n${(o.launch.firstHundredStrategy ?? ["Turn paid pilots into clear workflow proof", "Publish that proof in the buyer's existing community", "Expand channels only after repeat demand"]).map((s,i)=>`${i+1}. ${s}`).join("\n")}\n\n### Validation experiment\n${(o.launch.validationExperiment ?? []).map((s,i)=>`${i+1}. ${s}`).join("\n")}\n\n## Technical outline\n**Stack:** ${(o.technicalStack ?? []).join(", ")}\n\n**API dependencies:** ${(o.apiDependencies ?? []).join(", ")}\n\n## Final verdict\n**${o.scorecard.verdict}**\n\n${report.executiveSummary}\n\nWhat must be proven before scaling: ${o.launch.successMetric}\n`; }
export function reportToCsv(report: ValidationReport) { const o=report.opportunity; const headers=["Opportunity","Target customer","Score","Confidence","Verdict","Pricing","Build complexity","First customer channel","Top risk"]; const row=[o.name,o.targetCustomer,o.scorecard.total,o.scorecard.confidence,o.scorecard.verdict,o.pricing.pricePoint,o.mvp.buildComplexity,o.launch.firstCustomerChannel,o.risks[0]?.description??""]; return `${headers.join(",")}\n${row.map(value=>`"${String(value).replaceAll('"','""')}"`).join(",")}\n`; }
export function downloadExport(filename:string,content:string,mime:string){const url=URL.createObjectURL(new Blob([content],{type:mime}));const link=document.createElement("a");link.href=url;link.download=filename;link.click();URL.revokeObjectURL(url);}
